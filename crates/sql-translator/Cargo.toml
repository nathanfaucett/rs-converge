[package]
name = "sql-translator"
description = "SQL translator"
version = "0.1.0"
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lib]
path = "src/lib.rs"
name = "sql_translator"

[features]
default = ["std"]
std = ["query/std", "schema/std", "value/std", "sqlparser/std", "thiserror/std"]
wasm = [
  "tsify",
  "serde",
  "serde-wasm-bindgen",
  "wasm-bindgen",
  "query/wasm",
  "schema/wasm",
  "value/wasm",
]

[dependencies]
query = { path = "../query", default-features = false, features = [] }
schema = { path = "../schema", default-features = false, features = [] }
value = { path = "../value", default-features = false, features = ["serde_json"] }

sqlparser = { version = "0.62", default-features = false, features = [] }
thiserror = { version = "2.0", default-features = false, features = [] }
uuid = { version = "1.18", default-features = false }
serde_json = { version = "1.0", default-features = false, features = ["alloc"] }

# Optional TS generation + serde for tsify
tsify = { version = "0.5", default-features = false, features = [
  "js",
], optional = true }
serde = { version = "1.0", default-features = false, features = [
  "derive",
], optional = true }
serde-wasm-bindgen = { version = "0.6", default-features = false, optional = true }
wasm-bindgen = { version = "0.2", default-features = false, features = [
  "std",
], optional = true }

[dev-dependencies]
futures = { version = "0.3", default-features = false, features = ["executor"] }
